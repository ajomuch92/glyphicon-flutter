name: glyphicon
description: This packages contains the definition for 1370 icons used by old versions of Bootstrap (Glyphicon).
version: 0.3.0
homepage: https://github.com/ajomuch92/glyphicon-flutter

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  fonts:
    - family: Glyphicon
      fonts:
        - asset: fonts/Glyphicon.ttf
